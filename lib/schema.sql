CREATE TABLE Series (
id INTEGER PRIMARY KEY,
title TEXT,
author INTEGER,
sub-genre INTEGER
);

CREATE TABLE Sub-Genre (
id INTEGER PRIMARY KEY,
name TEXT
);

CREATE TABLE Author (
id INTEGER PRIMARY KEY,
name TEXT
);
